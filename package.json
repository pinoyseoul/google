{
  "name": "google-worker",
  "version": "1.0.0",
  "description": "Google Productivity Redirects via Cloudflare Workers",
  "main": "src/index.js",
  "scripts": {
    "deploy": "wrangler deploy",
    "start": "wrangler dev"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
